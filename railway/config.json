{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "node server.js",
    "healthcheck": {
      "path": "/health",
      "interval": 15,
      "timeout": 10,
      "grace_period": 120
    }
  }
}
